name: Check token scope
on:
  workflow_dispatch:
  push:

jobs:
  check-token:
    runs-on: ubuntu-latest
    steps:
    - name: print token
      run: |
        sudo apt update
        sudo apt install gh --yes
        gh auth status